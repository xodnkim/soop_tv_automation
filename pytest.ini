[pytest]
; -s: print() 로그를 항상 터미널에 출력 (pytest 기본 캡처 비활성화)
; -v: 각 테스트 이름과 결과를 자세히 출력
addopts = -s -v
testpaths = tests

; 프로젝트 루트를 sys.path에 추가 -> "from config import config", "from core.driver import ..." 등이
; 어디서 pytest를 실행하든 정상 동작하게 함 (pytest>=7 지원 옵션)
pythonpath = .
